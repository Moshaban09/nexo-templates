/**
 * Main Layout - Wrap your pages with this layout
 * Add header, footer, sidebar, etc.
 */

export default function MainLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
